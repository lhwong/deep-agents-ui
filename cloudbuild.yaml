steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --tag
      - us-central1-docker.pkg.dev/alpha-499407/alpha-repo/deep-agents-ui:latest
      - --build-arg
      - NEXT_PUBLIC_DEPLOYMENT_URL=/api/langgraph
      - --build-arg
      - NEXT_PUBLIC_ASSISTANT_ID=alpha-team
      - .

images:
  - us-central1-docker.pkg.dev/alpha-499407/alpha-repo/deep-agents-ui:latest

options:
#  machineType: E2_HIGHCPU_8
  logging: LEGACY
